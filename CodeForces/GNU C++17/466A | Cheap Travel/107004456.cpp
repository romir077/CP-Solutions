#include<bits/stdc++.h>
#include<iostream>
#define ll long long
#define rep(i,a) for(int i=0;i<a;i++)
#define int long long
#define di(a) int a;cin>>a;
#define dia di(a)
#define dix di(x)
#define diy di(y)
#define diz di(z)
#define dib di(b)
#define din di(n)
#define db double
#define all(x) x.begin(),x.end()
using namespace std;
signed main()
{
    din di(m) dia dib
    if(((double)b/(double)m)<=a){
    if(n%m==0){
        cout<<(n/m)*b;
    }
    else{
        if((n-(n/m)*m)*a<=b)
        cout<<((n/m)*b)+((n-(n/m)*m)*a);
        else
        cout<<ceil((double)n/(double)m)*b;
    }
    }
    else{
        cout<<n*a;
    }
    return 0;
}